ARG INFLUXDB

FROM influxdb:${INFLUXDB}

ENV INFLUXDB_DB=homee \
    INFLUXDB_ADMIN_USER=admin \
    INFLUXDB_ADMIN_PASSWORD=homeeDB_admin \
    INFLUXDB_USER=user \
    INFLUXDB_USER_PASSWORD=homeeDB_user \